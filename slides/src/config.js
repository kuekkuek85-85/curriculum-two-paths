// ─────────────────────────────────────────────────────────────
// 배포 후 채우는 값들 — 비어 있어도 앱은 깨지지 않고 기본 문구로 동작
// ─────────────────────────────────────────────────────────────

// 배포 주소 (예: "https://jasyul-2026.vercel.app")
// 비워두면 현재 접속 중인 주소(window.location.origin)를 자동 사용 —
// 즉 배포 직후 QR이 바로 올바른 주소를 가리킵니다.
export const DEPLOY_URL = "";

// S16 라이브 시연 링크 (새 탭으로 열림). 비우면 버튼 비활성 + "시연 링크 준비 중"
export const DEMO_URL = "";

// S16 버튼 아래 시연 대상 소개 문구. 비우면 기본 문구 사용
export const DEMO_INTRO = "";

// S17 시연 백업 캡처 2장 — public/ 폴더에 넣고 "/파일명.png" 형태로 지정.
// 비우면 회색 플레이스홀더 박스 표시
export const DEMO_CAPTURE_1 = "";
export const DEMO_CAPTURE_2 = "";

// S15-B 학습지 캡처 — 값을 넣으면 S15와 S16 사이에 슬라이드가 나타나고,
// 비워두면 해당 슬라이드는 렌더링에서 자동 제외됩니다(장수·진행바 자동 계산)
export const WORKSHEET_CAPTURE = "";

// ─────────────────────────────────────────────────────────────
// S14-B 퀴즈 상수 — 발표자의 실제 경험값에 맞게 수정
// ─────────────────────────────────────────────────────────────
export const QUIZ_OPTIONS = ["10분", "반나절", "이틀"];
export const QUIZ_ANSWER_INDEX = 0; // 0 = "10분"
